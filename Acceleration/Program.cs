using System;
using System.IO;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Acceleration
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            int n = 20;
            double[] a = new double[n];
            double totalTime = 1;
            double lastTime = 0;
            double dt = (totalTime - lastTime) / (n - 1);

            for (int i = 0; i < n; i++)
            {
                double t = lastTime + dt * i;
                a[i] = 3 * t * t * t + t * t + 5;
            }

            double X0 = 0;
            double V0 = 0;
            double X = AccelerationFuncs.destination(a, dt, X0, V0);

            Console.WriteLine("X ={0}", X);
            Console.Read();
            */

            
        }
    }
}
